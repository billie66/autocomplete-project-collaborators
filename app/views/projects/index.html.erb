<h2>Listing Projects</h2>
<ul>
  <% @projects.each do |project| %>
    <li>
      <%= project.name %>
      <%= link_to 'Add collaborators', project %>
    </li>
  <% end %>
</ul>
<p><%= link_to "Users", users_path %></p>