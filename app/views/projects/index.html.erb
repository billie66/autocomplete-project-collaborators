<h2>Projects</h2>
<ul>
  <% @projects.each do |project| %>
    <li>
      <%= project.name %>
      <%= link_to 'Add collaborators', project_path(:name => project.name) %>
    </li>
  <% end %>
</ul>
<p><%= link_to "Users", users_path %></p>